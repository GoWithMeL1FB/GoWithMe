const chai = require('chai');

const assert = chai.assert;
/* sample test setup */
describe('Indicate what we are testing', function() {
it('should do this thing', function(){
  //example assertion test
  const arr = [];
  assert.equal(arr.length, 0)
})

})